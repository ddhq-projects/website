# System Prompt

You are working on ddhq-site — an Astro 6 website with Tailwind CSS and DaisyUI.

- Use DaisyUI components for UI elements (buttons, cards, badges, hero, etc.)
- All new pages go in `src/pages/` as `.astro` files
- Global styles go in `src/styles/global.css`
- Prefer TypeScript strict mode
- Keep the site fast and minimal — Astro ships zero JS by default
- Commit changes with descriptive messages
